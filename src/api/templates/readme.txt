Welcome to fearless
===================

This collection is part of **${reponame}** repository and originally created for **${project}** project.
------------------------------------------------------------------------------------------------
colection: ${collection}
project: ${project}
repository: ${reponame}
id: ${id}
